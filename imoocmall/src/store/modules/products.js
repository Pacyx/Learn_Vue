import shop from '../../API/api'

// initial state
const state = {
  Price:"hello world1"
}

// getters
const getters = {
  getPrice: state => state.Price
}

// actions
const actions = {

}

// mutations
const mutations = {

}

export default {
  state,
  getters,
  actions,
  mutations
}
