import shop from '../../API/api'

// initial state
// shape: [{ id, quantity }]
const state = {
  Price:"hello world!2"
}

// getters
const getters = {
  getPrice : state => state.Price
}

// actions
const actions = {

}

// mutations
const mutations = {

}

export default {
  state,
  getters,
  actions,
  mutations
}
